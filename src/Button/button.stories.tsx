import React from 'react'
import { Button } from './Button'

export default {
  title: 'Button',
  component: Button,
}

export const button = () => {
  return <Button appearance="primary">Click me</Button>
}
